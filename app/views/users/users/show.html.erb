<div class="container container-size">
  <div class="row">
    <div class="col-2 mt-4">
      <%= render 'users/diaries/menu' %>
    </div>

    <div class="offset-1 col-6 my-5">
        <%= render 'users/users/user_info' %>
      <h2 class="mt-5 title-text" style="text-align:center;">
        <%= @user.name %>さんのペット
        <i class="fas fa-feather"></i>
      </h2>
        <%= render 'users/pets/pet_table' %>
        <div class="kaminari"><%= paginate @pets %></div>
    </div>

    <div class="col-3 mt-4">
      <%= render 'layouts/right_menu' %>
    </div>
  </div>
</div>